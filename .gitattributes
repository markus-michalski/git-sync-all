# Shell scripts must keep LF endings, including on Windows checkouts.
# A CRLF checkout makes bash fail with "$'\r': command not found".
* text=auto eol=lf

# Windows-native files keep CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
